# 更新日志

版本条目由 `pnpm release <版本>` 根据提交记录自动生成；发布前可直接编辑「未发布」小节润色，脚本会把它作为该版本的说明。

## 未发布

## v1.0.3 · 2026-09-05

### 新功能
- 自动检查更新改为后台静默：启动、回到前台时检查，6 小时内最多请求一次（每次约 4 KB），有新版本只在顶栏亮徽标，不弹窗、不抢终端焦点
- 更新拆成两步：先「下载并安装」，装好后可选「立即重启」或「稍后重启」，稍后重启时顶栏保留「重启完成更新」提示
- 点「稍后」后本次运行不再提醒同一版本

### 修复
- 更新对话框明确提示"重启会关闭所有终端会话"，避免误操作丢掉正在运行的命令

## v1.0.2 · 2026-09-05

### 新功能
- 更新弹窗按 Markdown 排版显示更新说明（小标题、条目、行内代码）

### 修复
- 应用内更新点「下载并重启」报错 "Cannot read private member"、无法完成更新
- 更新说明只显示本版改动，不再夹带安装包下载说明

## v1.0.1 · 2026-09-04

### 新功能
- 新增「自报终端身份」设置（iTerm2 / VS Code 兼容）：Devin CLI 等只认名单终端的工具不再在链接后面打印百分号编码的 URL
- 终端支持 OSC 8 超链接：悬停显示还原后的中文路径，点击 `file://` 链接在 Finder 中定位，其它链接用系统默认程序打开
- 子进程声明 `FORCE_HYPERLINK=1`，Claude Code 等 Node 系工具直接发可点击链接

### 修复
- 中文输入法下需要按 Shift 的全角标点（：？！“”等）打不出来

## v1.0.0 · 2026-09-04

首个公开版本：以 `~/.ssh/config` 为数据源的 SSH 桌面终端——分屏与面板拖拽、精选配色家族 + 600 余套配色、剪贴板图片/文件粘贴为路径、TUI 输出复制接回长行、SFTP 文件管理、快捷命令与批量执行、云账号带外开关机、`⌘P` 命令面板、应用内自动更新。
